@model IEnumerable<TaskPriorityDto>;

@{
    ViewData["Title"] = "Index";
    var i = 1;
}

<h1 style="text-align:center">Tasks priorities</h1>

<p>
    <a class="btn btn-primary btn-sm" asp-action="Add">Create New</a>
</p>

<div>
    <table class="table table-hover" aria-describedby="TaskCategory">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var priority in Model)
            {
                <tr>
                    <td>
                        @i
                    </td>
                    <td>
                        @Html.DisplayFor(c => priority.Name)
                    </td>
                    <td>
                        <a href="@Url.Action("Edit", "TaskPriority", new { id = priority.Id })" class="btn btn-primary btn-sm">
                            <i class="fa fa-mobile"></i> Edit
                        </a>
                        <a href="@Url.Action("Details", "TaskPriority", new { id = priority.Id })" class="btn btn-primary btn-sm">
                            <i class="fa fa-mobile"></i> Details
                        </a>
                        <a onclick="return confirm('Do you want to delete this row?')" href="@Url.Action("Delete", "TaskPriority", new { id = priority.Id })" class="btn btn-primary btn-sm">
                            <i class="fa fa-mobile"></i> Delete
                        </a>
                    </td>
                </tr>
                i++;
            }
        </tbody>
    </table>
</div>

