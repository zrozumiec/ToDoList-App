@model IEnumerable<TaskPriorityDto>;

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Add">Create New</a>
</p>

<div>
    <table class="table table-hover" aria-describedby="TaskCategory">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var priority in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(c => priority.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(c => priority.Name)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new {id = priority.Id}) |
                        @Html.ActionLink("Details", "Details", new {id = priority.Id}) |
                        @Html.ActionLink("Delete", "Delete", new {id = priority.Id}, new { onclick = "return confirm('Do you want to delete this row?');" }) |
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

